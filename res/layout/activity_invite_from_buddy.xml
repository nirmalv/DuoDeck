<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".InviteFromBuddyActivity" >

    <TextView
        android:id="@+id/invite_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="83dp"
        android:text="@string/invite_default" />

    <Button
        android:id="@+id/invite_accept"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/invite_label"
        android:layout_below="@+id/invite_label"
        android:layout_marginLeft="22dp"
        android:layout_marginTop="62dp"
        android:onClick="acceptInvite"
        android:text="@string/invite_accept_button" />

    <Button
        android:id="@+id/invite_decline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/invite_accept"
        android:layout_alignRight="@+id/invite_label"
        android:layout_marginRight="34dp"
        android:onClick="declineInvite"
        android:text="@string/invite_decline_button" />

</RelativeLayout>